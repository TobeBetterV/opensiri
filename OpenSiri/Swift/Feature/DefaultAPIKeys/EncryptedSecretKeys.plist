<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  openSiri 需要填入自己的密钥后再发布。

  上游 Easydict 在此提供了作者 tisfeng 自费的共享 API Key 与 Sentry DSN，
  迁移时已全部清空 —— 沿用会消耗上游配额，并把 openSiri 用户的崩溃数据
  发往上游账号。

  可用键：builtInAIAPIKey / builtInAIEndpoint / builtInAIModel /
          openAIAPIKey / openAIEndpoint / geminiAPIKey /
          caiyunToken / niutransAPIKey / sentryDSN
  值需为 AES 加密串，见 String+Encrypt.swift。
-->
<plist version="1.0">
<dict/>
</plist>
